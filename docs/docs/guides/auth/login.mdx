---
sidebar_position: 1
---

# Login

## Import

```jsx
import { EmailLoginForm } from "react-launchkit";
```

## Usage

import { EmailLoginForm, LaunchKitProvider } from "react-launchkit";
import { Box } from "@chakra-ui/react";

<LaunchKitProvider>
  <Box p="6" rounded="md" borderWidth={2}>
    <EmailLoginForm
      emailLabel={"Email"}
      emailPlaceholder="email@example.com"
      passwordLabel="Password"
      passwordPlaceholder="Your password"
      buttonText="Submit"
      onSubmit={async (values) => {
        alert(JSON.stringify(values));
      }}
    />
  </Box>
</LaunchKitProvider>
<Box h={20} />

```jsx
<EmailLoginForm
  emailLabel={"Email"}
  emailPlaceholder="email@example.com"
  passwordLabel="Password"
  passwordPlaceholder="Your password"
  buttonText="Submit"
  onSubmit={async (values) => {
    alert(JSON.stringify(values));
  }}
/>
```

## Props

| Prop Name           | Type     | Description                                                                                   |
| ------------------- | -------- | --------------------------------------------------------------------------------------------- |
| emailLabel          | string   | Label for the email input field                                                               |
| emailPlaceholder    | string   | Placeholder for the email input field                                                         |
| passwordLabel       | string   | Label for the password input field                                                            |
| passwordPlaceholder | string   | Placeholder for the password input field                                                      |
| buttonText          | string   | Text for the submit button                                                                    |
| onSubmit            | function | Function to be called when the form is submitted. It receives the form values as an argument. |
