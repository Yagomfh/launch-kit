const ROOT_PATH = '/'
const LOGIN_PATH = '/login'
const REGISTER_PATH = '/register'
const SETTINGS_PATH = '/settings'

const paths = {
  ROOT_PATH,
  LOGIN_PATH,
  REGISTER_PATH,
  SETTINGS_PATH
} as const

export default paths
