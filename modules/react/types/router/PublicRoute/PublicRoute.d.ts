import * as React from 'react';
import { ReactNode } from 'react';
type Props = {
    children: ReactNode;
};
declare function PublicRoute(props: Props): React.JSX.Element;
export default PublicRoute;
